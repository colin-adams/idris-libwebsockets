package libwebsockets

sourcedir = src

modules = WS.Context, WS.Protocol, WS.Handler, WS.Logging, WS.Daemon, WS.Extension, WS.Service, WS.Mount, WS.Vhost_options, WS.Uv, WS.Write

objs = lws.h

libs = websockets

pkgs = contrib, posix
